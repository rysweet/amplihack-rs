repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-merge-conflict
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files
        args: ["--maxkb=500"]

  - repo: local
    hooks:
      - id: artifact-guard
        name: amplihack artifact guard
        entry: bash -c 'CARGO_TARGET_DIR="${TMPDIR:-/tmp}/amplihack-precommit-target" cargo run --locked --bin amplihack -- hygiene artifact-guard --repo . --mode pre-commit'
        language: system
        pass_filenames: false
        always_run: true

      - id: check-toolchain-refs
        name: toolchain refs pinned to channel (issue #935)
        entry: scripts/check-toolchain-refs.sh
        language: system
        pass_filenames: false
        files: '(^\.github/workflows/.*\.ya?ml$|^rust-toolchain\.toml$)'

      - id: cargo-fmt
        name: cargo fmt --all --check
        entry: cargo fmt --all --check
        language: system
        pass_filenames: false
        files: '(\.rs|Cargo\.toml|Cargo\.lock)$'

      - id: cargo-clippy
        name: cargo clippy --all-targets -- -D warnings
        entry: bash -c 'CARGO_TARGET_DIR="${TMPDIR:-/tmp}/amplihack-precommit-target" cargo clippy --locked --all-targets -- -D warnings'
        language: system
        pass_filenames: false
        files: '(\.rs|Cargo\.toml|Cargo\.lock)$'

      # `--no-fail-fast` is not optional. Without it the first failing test binary
      # aborts the run, and one flake in an untouched crate is indistinguishable
      # from a real regression in your own change: an `amplihack-signal` flake
      # stopped a workspace run at 7463 of 8647 and reported nothing about the
      # 1184 tests that never executed. Run everything, then read the list.
      - id: cargo-test
        name: cargo test (workspace, skip heavy)
        entry: bash -c 'CARGO_TARGET_DIR="${TMPDIR:-/tmp}/amplihack-precommit-target" cargo test --workspace --locked --no-fail-fast -- --skip fleet_probe --skip kuzu --skip fleet::fleet_local --skip memory::kuzu'
        language: system
        pass_filenames: false
        stages: [pre-push]
        files: '(\.rs|Cargo\.toml|Cargo\.lock)$'
