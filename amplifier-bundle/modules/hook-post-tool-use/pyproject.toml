[project]
name = "amplifier-hook-post-tool-use"
version = "0.1.0"
description = "Post tool use hook - tool tracking, registry, and error detection"
requires-python = ">=3.11"
dependencies = ["amplifier-core"]

[project.entry-points."amplifier.modules"]
hook-post-tool-use = "amplifier_hook_post_tool_use"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
