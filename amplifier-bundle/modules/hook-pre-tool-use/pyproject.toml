[project]
name = "amplifier-hook-pre-tool-use"
version = "0.1.0"
description = "Pre-tool-use hook - blocks dangerous operations"
requires-python = ">=3.11"
dependencies = ["amplifier-core"]

[project.entry-points."amplifier.modules"]
hook-pre-tool-use = "amplifier_hook_pre_tool_use"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
