[project]
name = "amplifier-hook-session-stop"
version = "0.1.0"
description = "Session stop hook - learning capture and memory storage"
requires-python = ">=3.11"
dependencies = ["amplifier-core"]

[project.entry-points."amplifier.modules"]
hook-session-stop = "amplifier_hook_session_stop"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
