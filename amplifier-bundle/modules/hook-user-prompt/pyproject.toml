[project]
name = "amplifier-hook-user-prompt"
version = "0.1.0"
description = "User prompt hook - injects preferences and context"
requires-python = ">=3.11"
dependencies = ["amplifier-core"]

[project.entry-points."amplifier.modules"]
hook-user-prompt = "amplifier_hook_user_prompt"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
