# Dynamic Debugger Test Quickstart

Run the shipped E2E test:

```bash
cd amplifier-bundle/skills/dynamic-debugger/tests
bash test_e2e.sh
```

This verifies the skill's native shell helpers and DAP-MCP startup/cleanup path.
