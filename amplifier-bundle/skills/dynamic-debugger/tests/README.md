# Dynamic Debugger Tests

The shipped test surface is shell-based and validates the DAP-MCP lifecycle used
by the skill.

```bash
cd amplifier-bundle/skills/dynamic-debugger/tests
bash test_e2e.sh
```

Legacy Python unit tests are not shipped in `amplihack-rs`.
