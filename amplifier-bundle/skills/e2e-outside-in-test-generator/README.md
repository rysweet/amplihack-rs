# E2E Outside-In Test Generator

This skill writes end-to-end tests directly using the repository's existing tooling and includes `scripts/generate-e2e.sh` for starter plans/specs.

Use the main `SKILL.md` workflow: detect app type, identify journeys, create runnable tests, and validate with the repo's normal commands.
