digraph api {
  rankdir=LR; node[shape=box,fontsize=9];
  "amplihack CLI";
  "amplihack CLI" -> "append";
  "amplihack CLI" -> "auto_mode";
  "amplihack CLI" -> "builder";
  "amplihack CLI" -> "completions";
  "amplihack CLI" -> "cs_validate";
  "amplihack CLI" -> "doctor";
  "amplihack CLI" -> "fleet";
  "amplihack CLI" -> "hive_haymaker";
  "amplihack CLI" -> "hygiene";
  "amplihack CLI" -> "install";
  "amplihack CLI" -> "launch";
  "amplihack CLI" -> "lock";
  "amplihack CLI" -> "mcp_eval";
  "amplihack CLI" -> "memory";
  "amplihack CLI" -> "mode";
  "amplihack CLI" -> "multitask";
  "amplihack CLI" -> "new_agent";
  "amplihack CLI" -> "node";
  "amplihack CLI" -> "orch";
  "amplihack CLI" -> "orch";
  "amplihack CLI" -> "plugin";
  "amplihack CLI" -> "pr";
  "amplihack CLI" -> "query_code";
  "amplihack CLI" -> "recipe";
  "amplihack CLI" -> "reflect";
  "amplihack CLI" -> "remote";
  "amplihack CLI" -> "rustyclawd";
  "amplihack CLI" -> "session_tree";
  "amplihack CLI" -> "uvx_help";
  "amplihack CLI" -> "workflow";
  "amplihack CLI" -> "workflow";
}
